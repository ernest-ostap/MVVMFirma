<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieStanyMagazynoweView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdStanuMagazynowego}" />
                <DataGridTextColumn Header="Numer magazynu" Binding="{Binding IdMagazynu}"/>
                <DataGridTextColumn Header="Lokazlizacja magazynu" Binding="{Binding LokalizacjaMagazynu}"/>
                <DataGridTextColumn Header="Nazwa produktu" Binding="{Binding NazwaProduktu}"/>
                <DataGridTextColumn Header="Kategoria" Binding="{Binding Kategoria}"/>
                <DataGridTextColumn Header="Ilość" Binding="{Binding Ilosc}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
