<local:JedenViewBase x:Class="MVVMFirma.Views.NowaPlatnoscView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Margin="0,8,0,8" Content="Data płatności"/>
        <DatePicker Grid.Column="1" IsEnabled="true" Grid.Row="0" Margin="0,8,0,8" SelectedDate="{Binding Path=DataPlatnosci}" />

        <Label Grid.Column="0" Grid.Row="1" Margin="0,8,0,8" Content="Numer faktury"/>
        <TextBox Grid.Column="1" Grid.Row="1" IsEnabled="False" Margin="0,8,0,8" Text="{Binding Path=NumerFaktury, UpdateSourceTrigger=PropertyChanged}" />
        <Button Content="..." Grid.Column="2" Grid.Row="1" Margin="10" Command="{Binding ShowFaktury}"/>

        <Label Grid.Column="0" Grid.Row="2" Margin="0,8,0,8" Content="Kwota"/>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="0,10,0,6" Text="{Binding Path=Kwota, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Column="0" Grid.Row="3" Margin="0,8,0,8" Content="Metoda płatności"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="0,10,0,6" Text="{Binding Path=MetodaPlatnosci, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</local:JedenViewBase>
