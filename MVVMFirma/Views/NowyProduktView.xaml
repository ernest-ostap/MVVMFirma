<local:JedenViewBase x:Class="MVVMFirma.Views.NowyProduktView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Margin="0,8,0,8" Content="Nazwa"/>
        <TextBox Grid.Column="1" Grid.Row="0" Margin="0,8,0,8" Text="{Binding Path=Nazwa, UpdateSourceTrigger=PropertyChanged}" />
        
        <Label Grid.Column="0" Grid.Row="1" Margin="0,8,0,8" Content="Dostawca nazwa"/>
        <TextBox Grid.Column="1" Grid.Row="1" IsEnabled="False" Margin="0,8,0,8" Text="{Binding Path=NazwaDostawcy, UpdateSourceTrigger=PropertyChanged}" />
        <Button Content="..." Grid.Column="2" Grid.Row="1" Margin="10" Command="{Binding ShowDostawcy}"/>

        <Label Grid.Column="0" Grid.Row="2" Margin="0,8,0,8" Content="Dostawca NIP"/>
        <TextBox Grid.Column="1" Grid.Row="2" IsEnabled="False" Margin="0,8,0,8" Text="{Binding Path=DostawcaNIP, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Column="0" Grid.Row="3" Margin="0,8,0,8" Content="Kategoria"/>
        <ComboBox Grid.Column="1" Grid.Row="3" Margin="0,8,0,8" 
              ItemsSource="{Binding KategorieComboBoxItems}"
              SelectedValue="{Binding Path=IdKategorii, Mode=TwoWay}"
              DisplayMemberPath="Value"
              SelectedValuePath="Key"
              />
        <Label Grid.Column="0" Grid.Row="4" Margin="0,8,0,8" Content="Cena zakupu"/>
        <TextBox Grid.Column="1" Grid.Row="4" Margin="0,8,0,8" Text="{Binding Path=CenaZakupu, UpdateSourceTrigger=PropertyChanged}" />
        
        <Label Grid.Column="0" Grid.Row="5" Margin="0,8,0,8" Content="Cena sprzedaży"/>
        <TextBox Grid.Column="1" Grid.Row="5" Margin="0,8,0,8" Text="{Binding Path=CenaSprzedazy, UpdateSourceTrigger=PropertyChanged}" /> 
        
        <Label Grid.Column="0" Grid.Row="6" Margin="0,8,0,8" Content="Ilość"/>
        <TextBox Grid.Column="1" Grid.Row="6" Margin="0,10,0,6" Text="{Binding Path=Ilosc, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</local:JedenViewBase>
