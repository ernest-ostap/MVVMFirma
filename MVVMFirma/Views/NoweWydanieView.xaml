<local:JedenViewBase x:Class="MVVMFirma.Views.NoweWydanieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Margin="0,8,0,8" Content="Data wydania"/>
        <DatePicker Grid.Column="1" Grid.Row="0" Margin="10,8,-10,8" SelectedDate="{Binding Path=DataWydania}" />
        
        <Label Grid.Column="0" Grid.Row="1" Margin="0,8,0,8" Content="Klient"/>
        <ComboBox Grid.Column="1" Grid.Row="1" Margin="10,8,-10,8" 
                  ItemsSource="{Binding KlienciComboBoxItems}"
                  SelectedValue="{Binding Path=IdKlienta, Mode=TwoWay}"
                  DisplayMemberPath="Value"
                  SelectedValuePath="Key"
                  />

        <Label Grid.Column="0" Grid.Row="2" Margin="0,8,0,8" Content="Sposób transportu"/>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="10,10,-10,6" Text="{Binding Path=Transport, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</local:JedenViewBase>
